extends layout

block content
  h1 Edit Task

  if error
    p.error #{error}

  form(action=`/edit/${todo.id}` method="POST")
    input(type="text" name="task" value=todo.task placeholder="Update task")
    button(type="submit") Save

  a(href="/") Go Back
